/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["localhost",'storage.googleapis.com'], // Add other domains as needed
    },
  };
  
  export default nextConfig;
  